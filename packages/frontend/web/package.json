{
  "name": "frontend-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview",
        "docker:build": "node -e 'const s=require(\"./package.json\").name; const u=process.env.DOCKERHUB_USERNAME; const tag=process.env.TAG; const env=process.env.ENV_ALIAS; require(\"child_process\").execSync(`docker build -t ${u}/${s}:${tag} -t ${u}/${s}:${env} .`,{stdio:\"inherit\"})'",
    "docker:push":  "node -e 'const s=require(\"./package.json\").name; const u=process.env.DOCKERHUB_USERNAME; const tag=process.env.TAG; const env=process.env.ENV_ALIAS; require(\"child_process\").execSync(`docker push ${u}/${s}:${tag} && docker push ${u}/${s}:${env}`,{stdio:\"inherit\"})'",
     "deploy": "echo 'Deploying frontend-web'"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^6.22.3",
    "bootstrap": "^5.3.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "eslint": "^9.39.1",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.4.24",
    "globals": "^16.5.0",
    "vite": "^7.2.4"
  }
}